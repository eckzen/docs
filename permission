Locate the project directory

sudo chown -R www-data:eckzen (folder/file)
sudo chmod -R 776 (folder/file)

sudo adduser <username> www-data
sudo chown -R www-data:www-data /var/www
sudo chmod -R g+rw /var/www

Cannot access through cd command blank folder

solution:
	chmod ug+x blank

	sudo chmod -R ug+x filename
